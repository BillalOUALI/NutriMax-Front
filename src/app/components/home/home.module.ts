import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { HomeComponent } from './home.component';

@NgModule({
  declarations: [], // No declarations needed for standalone components
  //imports: [CommonModule, HomeComponent], // Import CommonModule and HomeComponent here
 // exports: [HomeComponent] // Export the component to be used elsewhere
})
export class HomeModule {}
